node_modules/
dist/
*.log
.DS_Store

# Android / Gradle
.gradle/
build/
local.properties
app/src/main/assets/kQSO/
.kotlin/

# Release signing (keystore lives outside git; CI gets it from secrets)
keystore/
*.jks
*.keystore

# Scratch inputs (source lists, icon drafts)
tmp/
